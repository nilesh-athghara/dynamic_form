//this file contains action blueprints responsible for causing a change in appstate through users interaction on the app
class ActionStoreHeight {
  final double height;
  ActionStoreHeight(this.height);
}

class ActionStoreWidth {
  final double width;
  ActionStoreWidth(this.width);
}
